[buildout]
extends =
  dr-instance.cfg
  sources-devel.cfg
  tests.cfg
  versions.cfg

[instance]

eggs +=
  anz.ecasclient
  reportek.authentication_overrides
  redis

zcml +=
  anz.ecasclient
  reportek.authentication_overrides


[versions]
anz.casclient = 1.0.1
