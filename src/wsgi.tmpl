[app:zope]
use = egg:Zope2#main
zope_conf = ${instance:location}/etc/zope.conf

[pipeline:main]
pipeline =
    egg:repoze.retry#retry
    egg:repoze.tm2#tm
    zope

[server:main]
use = egg:paste#http
host = 0.0.0.0
port = ${instance:http-address}
